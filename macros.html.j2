{% macro render_card(title, content, image_url=None) %}
  <div class="card">
    {% if image_url %}
      <img src="{{ image_url }}" alt="image">
    {% endif %}
    <h3>{{ title }}</h3>
    <p>{{ content }}</p>
  </div>
{% endmacro %}
